# Агрегированный файл для установки полного набора зависимостей.
# requirements.txt  -> ядро (production) + все опциональные провайдеры.
# Чтобы поставить только ядро без облачных/доп. бэкендов, используйте
#    pip install -r requirements-prod.txt
-r requirements-prod.txt
-r requirements-optional.txt
